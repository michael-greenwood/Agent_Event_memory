[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "causal_memory"
version = "0.1.0"
description = "Event-driven causal memory experiments"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["."]